using UnityEngine;

public class GameManager : MonoBehaviour
{
    void Start()
    {
        // Generate level
        
        // Spawn objects

        // Spawn waypoints

        // Spawn player/monster (start game)
    }
}
